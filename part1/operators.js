let isLoggedin = true;
let isActive = true;

if(isLoggedin && isActive) {
    console.log("User is active and logged in:"+ true);
} else {
    console.log(false);
}

/*
-- operators
&&
||
!
==
!=

arithmatic
+, -, *, /, %, ** (exponential)
*/
console.log(2 ** 3);